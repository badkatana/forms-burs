import styled from "@emotion/styled";
import { Typography } from "@mui/material";

export const NewsHeadline = styled(Typography)({
  fontSize: "2rem",
  color: "#783b38",
  fontStyle: "italic",
  fontWeight: "bold",
});
