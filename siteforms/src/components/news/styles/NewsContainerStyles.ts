import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const StyledContainerNews = styled(Box)({
  width: "80%",
  display: "flex",
  margin: "auto",
  flexWrap: "wrap",
  justifyContent: "center",
});
